{
    "name": "<%= _.slugify(appName) %>",
    "version": "0.0.0",<% if (node) { %>
    "dependencies": {
        "express": "~3.3.4",
        "express3-handlebars": "~0.4.1"<% if (mongodb) { %>,
        "mongodb": "~1.3.11"<% } if (redis) { %>,
        "redis": "~0.8.4"<% } %>
    },<% } %>
    "devDependencies": {
        "grunt": "~0.4.1",
        "grunt-contrib-watch": "~0.4.4",
        "grunt-contrib-sass": "~0.4.1",
        "grunt-component-build": "~0.3.0",
        "grunt-contrib-uglify": "~0.2.2"<% if (node) { %>,
        "grunt-nodemon": "~0.0.7",
        "grunt-concurrent": "~0.3.0"<% } %><% if (basic || static) { %>,
        "grunt-contrib-connect": "~0.3.0"<% } %>
    }
}